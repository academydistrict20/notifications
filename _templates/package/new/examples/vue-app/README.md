---
to: "<%= useVueComponents ? `packages/${h.changeCase.param(pkg)}/examples/vue-app/README.md` : null %>"
---
# Example Vue App

```bash
# 1. Install packages
npm i
# 2. Run dev server
npm run dev
```
